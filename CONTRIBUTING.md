# Contributing Guidelines

1. **Branching**: Branch off `master` using `feat/` or `fix/`.
2. **Linting**: Code must pass `ruff check .` and `mypy .`.
3. **Testing**: Add `pytest` cases for all new Python functions. Ensure React frontend builds with 0 TS errors.
